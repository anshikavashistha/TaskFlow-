DB_HOST=db
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=taskflow

JWT_SECRET=replace-with-a-long-random-secret
PORT=8080

# When true, inserts test@example.com + demo project/tasks after migrations (idempotent).
SEED=true
